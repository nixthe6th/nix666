# NIX CLI Setup
# Add this to your ~/.bashrc or ~/.zshrc:

# NIX toolkit
export PATH="$PATH:/path/to/nix666/scripts"
alias nix='nix'

# Optional: Show random quote on terminal startup
# quote() { node /path/to/nix666/quote.js 2>/dev/null; }
# quote
